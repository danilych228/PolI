# PolI
CLI AI Chat using Pollinations API

**Installing**
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip
git clone https://github.com/danilych228/PolI.git
cd PolI
pip3 install -r requirements.txt
```

**Using**
```bash
python3 poli.py
```
